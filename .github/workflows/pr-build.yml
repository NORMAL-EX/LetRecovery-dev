name: PR Build Check

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pr-build-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    name: Build Rust · ${{ matrix.name }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: PE端
            dir: PE端
          - name: 正常系统端
            dir: 正常系统端
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ${{ matrix.dir }}

      - name: Verify Cargo.lock is up to date
        working-directory: ${{ matrix.dir }}
        run: cargo metadata --locked --format-version 1 > metadata.json

      - name: Cargo check
        working-directory: ${{ matrix.dir }}
        run: cargo check --locked

      - name: Cargo build
        working-directory: ${{ matrix.dir }}
        run: cargo build --locked

      # 以下为代码质量检查，非阻塞（不影响构建结论），仅供参考
      - name: Cargo fmt check
        working-directory: ${{ matrix.dir }}
        run: cargo fmt --check
        continue-on-error: true

      - name: Cargo clippy
        working-directory: ${{ matrix.dir }}
        run: cargo clippy --locked --all-targets
        continue-on-error: true

  website:
    name: Build 官网
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 官网
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: 官网/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run type-check

      - name: Build
        run: npm run build
